import { memo } from "react";
import { EditUser } from "widgets";

const EditUserPage = memo(() => {
    return (
        <div>
            <EditUser />
        </div>
    );
});

export default EditUserPage;
